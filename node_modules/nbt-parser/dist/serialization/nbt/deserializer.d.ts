import { AbstractPayload } from "../../nbt/Payload";
import { BinaryCompression, Edition } from "../../types";
import { Tag, TagId } from "../../nbt/Tag";
export declare function deserializeNBTToTag(data: Uint8Array, edition: Edition, compression?: BinaryCompression | "auto"): Tag;
export declare function deserializeNBTToPayload(view: DataView, id: TagId, edition: Edition): [AbstractPayload<any>, number];
