import { Tag } from "../../nbt/Tag";
import { BinaryCompression, Edition } from "../../types";
import { AbstractPayload } from "../../nbt/Payload";
export declare function serializeTagToNBT(tag: Tag, edition: Edition, compression?: BinaryCompression): Uint8Array;
export declare function serializePayloadToNBT(payload: AbstractPayload<any>, edition: Edition): Uint8Array;
