import { BinaryCompression, Edition, SNBTSerializerOptions } from "../types";
import { AbstractPayload } from "./Payload";
export declare enum TagId {
    END = 0,
    BYTE = 1,
    SHORT = 2,
    INT = 3,
    LONG = 4,
    FLOAT = 5,
    DOUBLE = 6,
    BYTE_ARRAY = 7,
    STRING = 8,
    LIST = 9,
    COMPOUND = 10,
    INT_ARRAY = 11,
    LONG_ARRAY = 12
}
export declare class Tag {
    payload: AbstractPayload<any>;
    readonly root: boolean;
    private _name;
    constructor(name: string, payload: AbstractPayload<any>, root?: boolean);
    validate(name: string): void;
    copy(name?: string, payload?: AbstractPayload<any>, root?: boolean): Tag;
    equals(other: Tag): boolean;
    get name(): string;
    set name(name: string);
    toNBT(edition: Edition, binaryCompression?: BinaryCompression): Uint8Array;
    toJson(): any;
    toSNBT(compression?: Partial<SNBTSerializerOptions>): string;
}
